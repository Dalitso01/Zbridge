const userActivity = {
  completedSimulations: ["simulation1", "simulation2"],
  interests: ["finance", "leadership"],
};

export function getUserActivity() {
  return userActivity; // Replace with backend call if needed
}